services:
  - type: web
    name: healthcare-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --workers 3 --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13